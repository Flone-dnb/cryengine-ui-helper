# CRYENGINE UI Helper

TODO: screenshot

This tool eases up the process of generating `.gfx` and `.xml` for you UI `.swf` file.

# Features

- parses `.swf` to detect ActionScript version and use appropriate Scaleform version.